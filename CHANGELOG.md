# 📜 Журнал изменений и архитектуры проекта (Project Changelog & Architecture Log)

> **ДЛЯ РАЗРАБОТЧИКА И ВСЕХ AI (Gemini, Claude, GPT, Cursor, Copilot):**
> Данный файл является единым источником правды по истории изменений и архитектурным решениям в проекте **DevLearn**.
> **ПРАВИЛО:** Любое изменение в коде, структуре файлов, архитектуре или зависимостях **ОБЯЗАНО** фиксироваться в этом файле новой записью.

---

## 📌 Формат записи для AI и разработчика

```markdown
### [ГГГГ-ММ-ДД ЧЧ:ММ] Краткое название изменения
- **Автор:** AI (Gemini / Claude / GPT) | Разработчик (alisher)
- **Тип:** Архитектура | Новая фича | Рефакторинг | Багфикс | Зависимости
- **Затронутые модули/файлы:** `path/to/file1`, `path/to/file2`
- **Что сделано:** Описание изменений.
- **Архитектурные последствия:** Влияние на систему, стек, API или модели данных.
```

---

## 📝 История изменений

### [2026-09-11 12:47] Добавлены команды авторизации Expo из корня проекта (login, whoami, logout)
- **Автор:** AI (Antigravity) & Разработчик (alisher)
- **Тип:** Улучшение DX
- **Затронутые модули/файлы:** `package.json`, `mobile-app/package.json`, `CHANGELOG.md`
- **Что сделано:**
  - В корневой `package.json` и `mobile-app/package.json` добавлены команды `login`, `whoami` и `logout`.
  - Позволяет выполнять авторизацию в Expo (`npm run login`) напрямую из корня репозитория без перехода в подкаталог `mobile-app` и без глобальной установки `expo`.
- **Архитектурные последствия:** Упрощен процесс авторизации в Expo на удаленных серверах.

### [2026-09-11 12:40] Добавлен интеллектуальный скрипт scripts/show-qr.js (npm run qr / npm run pm2:qr)
- **Автор:** AI (Antigravity) & Разработчик (alisher)
- **Тип:** Новая фича | Улучшение DX
- **Затронутые модули/файлы:** `scripts/show-qr.js`, `package.json`, `CHANGELOG.md`
- **Что сделано:**
  - Создан скрипт `scripts/show-qr.js`, опрашивающий API ngrok (`127.0.0.1:4040`) и логи PM2 для автоматического извлечения `exp://` ссылки и отрисовки компактного QR-кода через `qrcode-terminal`.
  - В корневой `package.json` добавлены команды `npm run qr` и `npm run pm2:qr`.
- **Архитектурные последствия:** Разработчику больше не нужно искать ссылку в бесконечных логах PM2 — достаточно выполнить `npm run qr`.

### [2026-09-11 12:32] Оптимизация вывода QR-кода в PM2 и отключение CI-режима для туннеля
- **Автор:** AI (Antigravity) & Разработчик (alisher)
- **Тип:** Улучшение DX | Развертывание
- **Затронутые модули/файлы:** `ecosystem.config.js`, `package.json`, `DEPLOYMENT.md`, `CHANGELOG.md`
- **Что сделано:**
  - Удален параметр `CI: '1'` из `ecosystem.config.js`, приводивший к блокировке интерактивного режима Metro Bundler.
  - В команду `npm run pm2:logs:tunnel` добавлен флаг `--lines 80`, гарантирующий отображение ASCII QR-кода и ссылок `exp://...`, которые ранее вытеснялись стандартным лимитом PM2 в 15 строк.
  - Добавлена быстрая команда `npm run pm2:qr` (`tail -n 45 mobile-app/logs/pm2-tunnel-out.log`) для мгновенного просмотра только QR-кода и ссылок подключения.
  - Обновлена документация в `DEPLOYMENT.md`.
- **Архитектурные последствия:** Разработчик сразу видит QR-код и URL подключения в логах PM2 без ручного подбора параметров вывода.

### [2026-09-11 11:56] Отключение десктопного GUI-отладчика на headless сервере (EXPO_UNSTABLE_HEADLESS=1)
- **Автор:** AI (Antigravity) & Разработчик (alisher)
- **Тип:** Багфикс | Развертывание
- **Затронутые модули/файлы:** `mobile-app/package.json`, `ecosystem.config.js`, `CHANGELOG.md`
- **Что сделано:**
  - Во все скрипты запуска туннеля в `mobile-app/package.json` (`tunnel`, `start:tunnel`, `tunnel:prod`, `tunnel:web`) и в `ecosystem.config.js` добавлена переменная окружения `EXPO_UNSTABLE_HEADLESS=1`.
  - Устранен вызов `@react-native/debugger-shell` (Electron-приложения `react-native-devtools`), приводивший к ошибке отсутствия графической библиотеки `libatk-1.0.so.0` на Linux VPS серверах без десктопного окружения.
- **Архитектурные последствия:** Metro Bundler с туннелем ngrok запускается на headless серверах без ошибок, не требуя установки оконных библиотек Electron.

### [2026-09-11 11:36] Единые команды для одновременного запуска API, Mobile и Web (dev:all, dev:all:tunnel, prod:all)
- **Автор:** AI (Antigravity) & Разработчик (alisher)
- **Тип:** Новая фича | DX (Developer Experience)
- **Затронутые модули/файлы:** `package.json`, `mobile-app/package.json`, `README.md`, `DEPLOYMENT.md`, `CHANGELOG.md`
- **Что сделано:**
  - Обновлена команда `npm run dev` и добавлена `npm run dev:all` для одновременного запуска Backend API на порту 3001, генерации QR-кода Metro для смартфона (Expo Go) и автоматического открытия Web-клиента в браузере (`http://localhost:8081`).
  - Добавлена команда `npm run dev:all:tunnel`, объединяющая запуск API, Web-версии и глобального туннеля `--tunnel` для Expo Go.
  - В `mobile-app/package.json` добавлены вспомогательные скрипты `tunnel:web` и `tunnel`.
  - Добавлена продакшн-команда `npm run prod:all` для параллельного запуска Express-сервера (раздающего API и скомпилированный Web SPA на порту 3001) и Expo Go Mobile Tunnel.
  - Обновлена документация в `README.md` и `DEPLOYMENT.md`.
- **Архитектурные последствия:** Разработчику больше не нужно запускать несколько терминалов — все три платформы (API, Mobile через Expo Go и Web SPA) запускаются ровно одной командой как в dev, так и в prod.

### [2026-09-11 11:21] Добавление запуска мобильного приложения с флагом --tunnel для Expo Go
- **Автор:** AI (Antigravity) & Разработчик (alisher)
- **Тип:** Новая фича | Развертывание | Зависимости
- **Затронутые модули/файлы:** `mobile-app/package.json`, `package.json`, `ecosystem.config.js`, `DEPLOYMENT.md`, `README.md`, `CHANGELOG.md`
- **Что сделано:**
  - В `mobile-app/package.json` добавлены скрипты `tunnel` (`expo start --tunnel`), `start:tunnel` и `tunnel:prod` (`expo start --tunnel --no-dev --minify`).
  - Пакет `@expo/ngrok` перенесен из `devDependencies` в `dependencies` в `mobile-app/package.json`, гарантируя его наличие при продакшн-установке зависимостей на сервере (`npm install --omit=dev`).
  - В корневом `package.json` добавлены команды быстрого доступа: `npm run dev:mobile:tunnel`, `npm run mobile:tunnel`, `npm run dev:tunnel` (одновременный старт API + Expo Tunnel), `npm run tunnel:prod`, а также команды PM2 (`pm2:start:tunnel`, `pm2:logs:tunnel`).
  - В `ecosystem.config.js` добавлен процесс `devlearn-mobile-tunnel` для демонизации раздачи Expo Go через PM2 в production.
  - В `DEPLOYMENT.md` и `README.md` описаны инструкции по запуску тоннеля и считыванию QR-кода на сервере.
- **Архитектурные последствия:** Обеспечена возможность запускать Expo Metro bundler на удаленном VPS/сервере в проде и подключаться к приложению через приложение Expo Go из интернета (через ngrok-туннель) без необходимости быть в одной Wi-Fi сети с сервером.

### [2026-09-10 16:44] Полное удаление сборки APK: удаление eas.json и скрипта build:apk
- **Автор:** AI (Gemini) & Разработчик (alisher)
- **Тип:** Архитектура | Рефакторинг
- **Затронутые модули/файлы:** `mobile-app/eas.json` (удален), `package.json`, `mobile-app/package.json`, `CHANGELOG.md`
- **Что сделано:**
  - По требованию разработчика полностью исключена функциональность сборки Android APK.
  - Удален конфигурационный файл сборщика Expo Application Services [`mobile-app/eas.json`](file:///home/alisher/my_projects/dev_learn/mobile-app/eas.json).
  - Из скриптов корневого [`package.json`](file:///home/alisher/my_projects/dev_learn/package.json) и [`mobile-app/package.json`](file:///home/alisher/my_projects/dev_learn/mobile-app/package.json) удалена команда `build:apk`.
- **Архитектурные последствия:** Проект полностью избавлен от неиспользуемого инструментария нативной Android-сборки; акцент сосредоточен на кроссплатформенном Web SPA и API.

### [2026-09-10 16:38] Удаление Systemd-юнита: фокус продакшн-стека исключительно на PM2 и Nginx
- **Автор:** AI (Gemini) & Разработчик (alisher)
- **Тип:** Архитектура | Рефакторинг
- **Затронутые модули/файлы:** `deploy/systemd/devlearn.service` (удален), `scripts/prod-check.js`, `DEPLOYMENT.md`, `CHANGELOG.md`
- **Что сделано:**
  - По запросу разработчика удален Systemd service (`deploy/systemd/devlearn.service`) и директория `deploy/systemd`, так как управление демонизацией и автозапуском в Linux возложено на PM2 (`pm2 startup` / `pm2 save`).
  - В скрипте предстартовой проверки `scripts/prod-check.js` (`npm run check:prod`) список обязательных файлов развертывания скорректирован до PM2 (`ecosystem.config.js`) и Nginx (`deploy/nginx/devlearn.conf`).
  - В руководстве [`DEPLOYMENT.md`](./DEPLOYMENT.md) исключен раздел Systemd, а стек полностью унифицирован под PM2 + Nginx.
- **Архитектурные последствия:** Устранены дублирующие конфигурации демонизации; на сервере используется единый стандарт управления процессами через PM2.

### [2026-09-10 16:30] Удаление Docker и оптимизация деплоя под нативный Linux VPS (PM2, Nginx, Systemd)
- **Автор:** AI (Gemini) & Разработчик (alisher)
- **Тип:** Архитектура | Рефакторинг
- **Затронутые модули/файлы:** `Dockerfile` (удален), `docker-compose.yml` (удален), `.dockerignore` (удален), `package.json`, `scripts/prod-check.js`, `DEPLOYMENT.md`, `README.md`, `CHANGELOG.md`
- **Что сделано:**
  - По запросу разработчика полностью удалены файлы Docker-инфраструктуры (`Dockerfile`, `docker-compose.yml`, `.dockerignore`).
  - Из корневого `package.json` удалены команды `docker:build`, `docker:up`, `docker:down`.
  - Обновлен CLI-скрипт предстартовой проверки `scripts/prod-check.js` (`npm run check:prod`) — проверка файлов развертывания теперь ориентирована на нативные инструменты: PM2 (`ecosystem.config.js`), Nginx (`deploy/nginx/devlearn.conf`), Systemd (`deploy/systemd/devlearn.service`).
  - Руководство по развертыванию [`DEPLOYMENT.md`](./DEPLOYMENT.md) и [`README.md`](./README.md) переработаны с фокусом на запуск через PM2 + Nginx Reverse Proxy + Let's Encrypt SSL без контейнеров.
- **Архитектурные последствия:** Стек упрощен, снижены требования к серверу (нет накладных расходов на Docker Engine/демон), развертывание ориентировано на чистый Node.js с PM2.

### [2026-09-10 16:25] Полная подготовка проекта к Production Server: безопасность API, All-in-One SPA раздача, Docker, PM2, Nginx и диагностика
- **Автор:** AI (Gemini) & Разработчик (alisher)
- **Тип:** Архитектура | Безопасность | Развертывание | Зависимости
- **Затронутые модули/файлы:** `api/src/app.js`, `api/src/config/env.js`, `api/src/config/database.js`, `api/server.js`, `api/src/controllers/healthController.js`, `api/package.json`, `package.json`, `mobile-app/package.json`, `.gitignore`, `.env.example`, `api/.env.example`, `ecosystem.config.js`, `Dockerfile`, `docker-compose.yml`, `.dockerignore`, `deploy/nginx/devlearn.conf`, `deploy/systemd/devlearn.service`, `scripts/prod-check.js`, `DEPLOYMENT.md`, `README.md`, `CHANGELOG.md`
- **Что сделано:**
  - **Харденинг и безопасность Express REST API:**
    - Установлены и интегрированы middleware: `helmet` (HTTP-заголовки безопасности), `compression` (Gzip/Brotli сжатие трафика), `express-rate-limit` (защита эндпоинтов `/api` от DoS и брутфорса, 300 запросов за 15 мин).
    - Настроен `trust proxy` для корректной работы за Nginx / Cloudflare / Traefik.
    - Ограничен размер тела JSON-запросов (`100kb`) для предотвращения memory exhaustion атак.
    - Добавлены предупреждения о дефолтном `JWT_SECRET` в production-режиме.
    - Перехват критических ошибок процесса `uncaughtException` и `unhandledRejection` с безопасным Graceful Shutdown.
  - **Оптимизация и отказоустойчивость SQLite (better-sqlite3):**
    - Включен таймаут блокировки `busy_timeout = 5000` (предотвращает ошибки `SQLITE_BUSY` при одновременных запросах).
    - Установлен безопасный режим синхронизации `synchronous = NORMAL` в связке с WAL.
    - Добавлено автоматическое рекурсивное создание директории базы данных при нестандартных путях (`DB_PATH`).
  - **Расширенный Healthcheck эндпоинт (`/api/health`):**
    - В ответ добавлены показатели потребления оперативной памяти (`memory.rssMb`, `heapUsedMb`, `heapTotalMb`), версия Node.js, окружение (`NODE_ENV`) и статус SQLite WAL.
  - **Архитектура All-in-One: Раздача Web SPA прямо из Express:**
    - Реализована автоматическая раздача статичного бандла `mobile-app/dist` сервером Express с кэшированием статики (`maxAge: 7d`) и SPA-fallback на `index.html` (с полной поддержкой маршрутизации Express 5).
    - Если статика не собрана, `/` возвращает информационный JSON API статус.
  - **Инфраструктура контейнеризации (Docker & Docker Compose):**
    - Создан оптимизированный многоэтапный `Dockerfile` (Stage 1: компиляция React Native Web через `npx expo export -p web`, Stage 2: чистый рантайм `node:22-bookworm-slim` с непривилегированным пользователем `node`, healthcheck и volume `/app/data` для базы данных).
    - Создан `docker-compose.yml` с именованным томом `devlearn_db_data` и автоматическим перезапуском `unless-stopped`.
    - Добавлен `.dockerignore` для ускорения сборки контекста.
  - **Инфраструктура для прямого запуска на VPS:**
    - Создан `ecosystem.config.js` для менеджера процессов PM2 (fork-режим, лимит памяти 350M, раздельные логи).
    - Создан готовый конфиг Nginx (`deploy/nginx/devlearn.conf`) с кэшированием ассетов Expo, Gzip и обратным проксированием.
    - Создан юнит Systemd (`deploy/systemd/devlearn.service`).
  - **Инструмент самодиагностики (Pre-flight check):**
    - Создан CLI-скрипт `scripts/prod-check.js` (`npm run check:prod`), проверяющий совместимость Node.js (>= 18), валидность `.env`, безопасность `JWT_SECRET`, здоровье базы SQLite и наличие сборки Web SPA с цветным консольным отчетом.
  - **Документация и переменные окружения:**
    - Созданы `.env.example` (в корне и `api/`) с подробными комментариями.
    - Создан подробный гайд [`DEPLOYMENT.md`](./DEPLOYMENT.md) с инструкциями по Docker, PM2, Nginx, SSL Certbot, безопасным горячим бэкапам SQLite и обновлению.
    - Обновлен `.gitignore` (защита секретов, временных файлов WAL и логов) и корневой `package.json` (команды `build:web`, `start:prod`, `check:prod`, `pm2:*`, `docker:*`).
- **Архитектурные последствия:** Проект полностью унифицирован и готов к мгновенному развертыванию на любом боевом Linux VPS сервере как в Docker-контейнере одной командой (`docker compose up -d`), так и в классическом стеке (Node.js + PM2 + Nginx + Let's Encrypt). Все исходные функции мобильного клиента и API сохранены на 100%.

### [2026-09-08 17:30] Редизайн кнопок действий в карточках медиа: Neo-Brutalist стиль, контекстные названия и выравнивание теней
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** UI/UX / Рефакторинг
- **Затронутые модули/файлы:** `mobile-app/src/screens/MediaScreen.js`, `mobile-app/src/constants/translations.js`, `CHANGELOG.md`
- **Что сделано:**
  - **Устранена блеклая плоская кнопка с косой чертой («Смотреть / Открыть»):** вместо невнятного текста внедрена динамическая подпись и соответствующая иконка под каждый тип ресурса (`getMediaActionInfo`).
  - **Контекстные понятные действия:**
    - Курсы: *«Открыть курс ↗»* / *«Kursni ochish ↗»* с иконкой `book-open`.
    - Видео: *«Смотреть видео ↗»* / *«Videoni ko'rish ↗»* с иконкой `play`.
    - Подкасты: *«Слушать подкаст ↗»* / *«Podkastni tinglash ↗»* с иконкой `headphones`.
    - Дайджесты: *«Читать дайджест ↗»* / *«Dayjestni o'qish ↗»* с иконкой `file-text`.
  - **Премиальный Neo-Brutalism визуальный стиль:**
    - Кнопка переведена на фирменный желтый акцент `#FDE047` с 2px черной рамкой и жесткой 2.5px оффсет-тенью `#000` (в тон кнопкам в каталоге уроков).
    - Добавлена стрелочка внешнего перехода `arrow-up-right`.
    - Кнопка копирования `mediaCopyBtn` выровнена по высоте (42px), скруглению (10px) и оффсет-тени (2.5px) в единую гармоничную группу.
- **Архитектурные последствия:** Карточки медиа-ресурсов получили завершённый, контрастный и наглядный вид без визуального разнобоя.

### [2026-09-08 17:22] Редизайн инфо-модалки «Открытые ресурсы» в Медиа-хабе: маскот Dotty, языковые пиллы и Neo-Brutalist карточки
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** UI/UX / Рефакторинг
- **Затронутые модули/файлы:** `mobile-app/src/screens/MediaScreen.js`, `CHANGELOG.md`
- **Что сделано:**
  - **Единый стандарт с каталогом уроков:** инфо-модалка «Открытые ресурсы» в `MediaScreen.js` обновлена до актуальной дизайн-системы платформы.
  - **Интеграция талисмана Halftone Dotty:** добавлен блок с анимированным маскотом `AnimatedDotty` и советом о подборках лучших видео/подкастов без расхода локальной памяти смартфона.
  - **4 визуальные карточки с бейджами:**
    1. *«100% Бесплатно и Открыто»* со значком `NO PAYWALL` и гарантией отсутствия скрытых подписок.
    2. *«Знания на 3 языках»* с яркими разноцветными плашками `[O'ZBEK]`, `[РУССКИЙ]`, `[ENGLISH]`.
    3. *«Прямой запуск в плеере»* с акцентом на просмотр в нативном приложении YouTube с 4K и ускорением.
    4. *«Кураторский отбор инженеров»* с акцентом на топовые курсы (Harvard CS50, Google) и проверенных авторов.
  - **UX-полировка:** интерактивная кнопка *«🚀 Понятно, к просмотру!»* / *«🚀 Tushunarli, ko'rishga!»* с иконкой `check-circle`, закрытие по тапу вне модалки и плавная анимация крестика.
- **Архитектурные последствия:** Синхронизация визуального языка и UX всех вспомогательных модальных окон в приложении под эстетику Neo-Brutalism.

### [2026-09-08 17:15] Редизайн инфо-модалки «Методология уроков»: живой маскот Dotty, градация Junior ➔ Senior и Neo-Brutalist карточки
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** UI/UX / Рефакторинг
- **Затронутые модули/файлы:** `mobile-app/src/screens/LessonsCatalogScreen.js`, `CHANGELOG.md`
- **Что сделано:**
  - **Устранён серый «сухой» текст:** вместо монотонного текстового абзаца модалка превращена в богато оформленный интерактивный гайд по инженерному подходу платформы.
  - **Интеграция талисмана Halftone Dotty:** в шапке модалки добавлен блок `AnimatedDotty` с советом талисмана о пользе уроков без лишней воды.
  - **4 структурированные карточки методологии:**
    1. *«3 инженерных уровня»* с визуальными разноцветными пиллами `[JUNIOR]` ➔ `[MIDDLE]` ➔ `[SENIOR]`.
    2. *«Реальный Production-код»* с иконкой кода и акцентом на отсутствие оторванной абстрактной теории.
    3. *«Архитектура под капотом»* с иконкой процессора и описанием рантайма, GC, памяти и Event Loop.
    4. *«Инсайты для собеседований»* с иконкой награды и фокусом на вопросах BigTech/FAANG и частых ловушках.
  - **UX-улучшения:** добавлено закрытие по клику вне модального окна (backdrop overlay), анимированный крестик `spin90` и акцентная нео-брутальная кнопка завершения *«🚀 Понятно, к урокам!»* с поддержкой мультиязычности (RU / UZ).
- **Архитектурные последствия:** Приведение инфо-модалок каталога уроков к общему премиальному стилю платформы и дизайн-системе Neo-Brutalism.

### [2026-09-08 17:10] Фикс адаптивных шапок каталогов и экранов: запрет переноса строк (no-wrap) и аккуратное усечение многоточием (...)
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** UI/UX / Багфикс
- **Затронутые модули/файлы:** `mobile-app/src/screens/LessonsCatalogScreen.js`, `mobile-app/src/screens/CollectionScreen.js`, `mobile-app/src/screens/MediaScreen.js`, `mobile-app/src/screens/LessonScreen.js`, `CHANGELOG.md`
- **Что сделано:**
  - **Запрет переноса строк в заголовках шапок:** для компонентов `collectionHeaderTitle` во всех экранах каталогов и коллекций добавлены свойства `numberOfLines={1}` и `ellipsizeMode="tail"`.
  - **Адаптивное ограничение ширины бейджа:** для `collectionHeaderBadge` установлено `maxWidth: '62%'`, `alignItems: 'center'`, `justifyContent: 'center'`, а для текста — `textAlign: 'center'`.
  - **Устранение коллизий и сжатия навигационных кнопок:** длинные заголовки (например, *«Интерактивные Уроки & Документация»*) на экранах смартфонов больше не выталкивают соседние кнопки «Назад» и «Инфо», а плавно усекаются многоточием в конце (`Интерактивные Уроки & Док...`).
- **Архитектурные последствия:** Единый стандарт адаптивного заголовка во всех экранах приложения с гарантией целостности верхнего навигационного бара на любых разрешениях.

### [2026-09-08 17:00] Редизайн карточки паспорта в «О приложении»: контрастный белый фон, крупный логотип и чёткая иерархия
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** UI/UX / Рефакторинг
- **Затронутые модули/файлы:** `mobile-app/src/modals/AboutModal.js`, `mobile-app/src/constants/translations.js`, `CHANGELOG.md`
- **Что сделано:**
  - **Устранён визуальный конфликт «жёлтое на жёлтом»:** карточка паспорта приложения переведена с жёлтого `#FFD02F` на чистый контрастный белый фон (`#FFFFFF`) с сохранением нео-брутальной 2.5px границы и 4px оффсет-тени.
  - **Полноразмерная центрированная презентация логотипа:**
    - Убрано тесное размещение «маскот в квадратике + уменьшенный логотип сбоку».
    - Официальный логотип `dev.learn` со слоганом `> COMPILE YOUR SKILLS` вынесен на отдельную просторную плашку (`width: 210, height: 56`) по центру карточки с элегантным нижним разделителем `#E4E4E7`.
    - Под логотипом размещён чёткий подзаголовок тёмно-серым шрифтом `#52525B`.
  - **Разделение сущностей Бренда и Талисмана:**
    - Исключено дублирование: маскот Dotty теперь представлен исключительно в своей персональной карточке «Талисман платформы: Halftone Dotty» ниже.
  - **Актуализация текстов:** в описании талисмана в `translations.js` устаревшее название `DevPocket` заменено на актуальное `DevLearn`.
- **Архитектурные последствия:** Карточка паспорта приложения получила образцовый контраст, логотип читается идеально четко, устранен визуальный хаос.

### [2026-09-08 16:56] Редизайн шапки главного экрана: чёткий Neo-Brutalism бейдж, типографика dev.learn и очистка лишних кнопок
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** UI/UX / Рефакторинг
- **Затронутые модули/файлы:** `mobile-app/src/screens/HomeScreen.js`, `CHANGELOG.md`
- **Что сделано:**
  - **Устранена проблема размытого мелкого логотипа в шапке:** убран уменьшенный широкий растровый файл с нечитаемым 5-пиксельным слоганом, который создавал визуальный шум.
  - **Внедрён премиальный Neo-Brutalism заголовок:**
    - Использован крупный чёткий бейдж `logo-badge.png` (желтая карточка ROOT с точечной стрелкой).
    - Рядом выведена нативная чёткая векторная типографика: `dev`<Text style={{ color: '#EAB308' }}>.</Text>`learn` (`fontSize: 22`, `fontWeight: '900'`, цвет `#000` с фирменной желтой точкой).
  - **Очистка от визуального мусора в верхней строке:**
    - Удалена избыточная кнопка поиска из верхнего ряда (поскольку прямо под Hero-блоком уже расположена полноразмерная поисковая строка со значком `#`).
    - Верхняя строка теперь идеально сбалансирована: слева интерактивный логотип `dev.learn`, справа только переключатель языка (RU/UZ) и кнопка настроек.
- **Архитектурные последствия:** Устранены теснота и дублирование действий на смартфонах и ПК. Шапка выглядит аккуратно, премиально и строго в концепции Neo-Brutalism.

### [2026-09-08 16:45] Интеграция официального логотипа DevLearn: брендинг, мета-теги, фавиконки и UI
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Новая фича / Брендинг / SEO / UI/UX
- **Затронутые модули/файлы:** `mobile-app/assets/`, `mobile-app/public/`, `mobile-app/app.json`, `mobile-app/web/index.html`, `mobile-app/src/screens/HomeScreen.js`, `mobile-app/src/modals/AboutModal.js`, `mobile-app/src/modals/SettingsModal.js`, `CHANGELOG.md`
- **Что сделано:**
  - **Генерация полного комплекта графических ассетов из исходника логотипа (`svgviewer-png-output.png`):**
    - `logo.png` / `logo-horizontal.png` — официальный горизонтальный логотип бренда (`dev.learn` + бейдж `ROOT` + слоган `> COMPILE YOUR SKILLS`).
    - `logo-badge.png` — компактный желтый Neo-Brutalist бейдж с точечной стрелкой и плашкой `ROOT`.
    - `icon.png` (1024x1024) — основная иконка приложения для мобильных платформ и сторов.
    - `favicon.png` и `favicon.ico` (48x48, 64x64) — фирменная иконка для браузерной вкладки.
    - `android-icon-foreground.png` и `android-icon-background.png` — адаптивная иконка Android.
    - `splash-icon.png` (1024x1024) — сплэш-скрин загрузки приложения.
    - `og-image.png` (1200x630) — брендированная карточка предпросмотра соцсетей (OpenGraph / Twitter Cards).
  - **Интеграция мета-тегов и SEO (`public/index.html`):**
    - Настроен полноценный `index.html` в Expo `public/` с Open Graph (`og:image`, `og:title`, `og:description`, `og:type`), Twitter Card (`summary_large_image`), favicon, apple-touch-icon и `theme-color: #FFD02F`.
    - Обновлен `app.json`: актуализированы имя приложения `"DevLearn"` и пути к новым фирменным ассетам.
  - **Внедрение логотипа в ключевые экраны интерфейса:**
    1. **Главный экран (`HomeScreen.js`):** логотип размещен в верхней навигационной панели (`topNavRow`) слева; клик по логотипу сбрасывает фильтр/поиск и возвращает к началу.
    2. **Модалка «О платформе» (`AboutModal.js`):** текстовый заголовок заменен на полноцветный графический логотип в карточке паспорта приложения.
    3. **Модалка «Настройки» (`SettingsModal.js`):** добавлен футер с логотипом и официальным слоганом базы знаний.
- **Архитектурные последствия:** Проект получил единую визуальную айдентику, профессиональный брендинг во всех поддерживаемых средах (Web, PWA, Android, iOS) и корректное социальное отображение в превью ссылок.

### [2026-09-08 16:35] Полноэкранная ширина сетки (width full): 4 колонки на больших экранах, 2 на мобильных
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** UI/UX / Рефакторинг
- **Затронутые модули/файлы:** `mobile-app/src/screens/HomeScreen.js`, `mobile-app/src/components/home/AnimatedFolderCard.js`, `CHANGELOG.md`
- **Что сделано:**
  - **Устранено пустое пространство справа и ложный перенос карточки:** ранее из-за несовпадения теоретического `1160px` с реальной шириной внутреннего контейнера (учёт скроллбара и бордеров окна) 4-я карточка не помещалась в ряд и переносилась, оставляя 25% пустоты справа.
  - **Математически точный 100% Full-Width расчёт (`calc`):**
    - Для Web внедрена формула `width: calc((100% - ${totalGap}px) / ${numColumns})`, которая силами браузера гарантирует строгое 100% заполнение всей доступной ширины ряда стык в стык с поисковой строкой.
    - Для Native подключён слушатель `onLayout` на контейнере `cardsGrid` с автоматическим делением реальной ширины.
  - **Четкие брейкпоинты по требованию:**
    - **Большие экраны (`width >= 768`):** строго **4 карточки в ряд** (`colGap: 16`).
    - **Мобильные устройства (`width < 768`):** строго **2 карточки в ряд** (`colGap: 12`).
  - `styles.cardsGrid` получил свойство `width: '100%'`, а `AnimatedFolderCard` очищен от статических ширин.
- **Архитектурные последствия:** Сетка папок стала идеально монолитной, занимает 100% ширины контентной области без каких-либо зазоров или пустот справа на любых мониторах и смартфонах.

### [2026-09-08 16:30] Порядковая сетка коллекций без разброса flex: устранение пробела в последнем ряду
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** UI/UX / Багфикс
- **Затронутые модули/файлы:** `mobile-app/src/screens/HomeScreen.js`, `mobile-app/src/components/home/AnimatedFolderCard.js`, `CHANGELOG.md`
- **Что сделано:**
  - **Устранена классическая проблема flex space-between:** ранее при 10 папках в сетке из 4 колонок карточки 3-го ряда (№9 и №10) раскидывались по противоположным углам экрана с огромной пустотой по центру.
  - **Порядковое выравнивание слева направо (`flex-start` + `columnGap` / `rowGap`):** контейнер `cardsGrid` переведен на выравнивание `justifyContent: 'flex-start'` со строгим порядковым заполнением.
  - **Пиксельно-точный адаптивный расчет ширины карточек (`cardWidth`):** ширина каждой карточки теперь динамически высчитывается в `HomeScreen.js` исходя из реальной ширины контейнера (`containerWidth`), числа колонок (4 на ПК, 3 на планшетах, 2 на смартфонах) и точного горизонтального отступа `columnGap` (16px / 14px / 12px).
  - Теперь 10-я карточка («Новости & Тренды») стоит строго рядом с 9-й («Тулзы & Утилиты») в естественном порядке (1-я и 2-я колонки), а 3-я и 4-я колонки аккуратно остаются свободными.
- **Архитектурные последствия:** Сетка папок на главном экране стала визуально стабильной и гармоничной на любых экранах (от мобильных телефонов до широкоформатных 4K мониторов).

### [2026-09-08 16:23] Светлая тема для блоков кода и улучшенная фильтрация новостей в коллекциях
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** UI/UX / Рефакторинг
- **Затронутые модули/файлы:** `mobile-app/src/screens/CollectionScreen.js`, `CHANGELOG.md`
- **Что сделано:**
  - **Переход на светлую тему кода:** убран темный контрастный фон `#0F172A` с бирюзовым текстом, который выбивался из общего стиля карточек. Блоки кода переведены на чистую светлую тему Neo-Brutalism: мягкий светлый фон `#F8FAFC`, плашка заголовка `#F1F5F9` с разделителем `#CBD5E1`, четкий темный моноширинный текст `#0F172A` с четкой 2px черной рамкой.
  - **Устранение ложного распознавания новостей как кода (`resolveCardData`):** расширен эвристический парсер, чтобы карточки новостей и маркированные списки с буллетами (`•`) никогда ошибочно не классифицировались как сниппеты кода с кнопками копирования.
- **Архитектурные последствия:** Кодовые блоки приведены к единому визуальному стандарту приложения с высокой читаемостью и гармоничным сочетанием с белыми карточками.

### [2026-09-08 16:20] Контекстный редизайн карточек коллекций: удаление нерелевантного копирования и умные действия
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** UI/UX / Рефакторинг / Архитектура
- **Затронутые модули/файлы:** `mobile-app/src/screens/CollectionScreen.js`, `mobile-app/src/constants/collectionsData.js`, `mobile-app/src/constants/translations.js`, `mobile-app/App.js`, `CHANGELOG.md`
- **Что сделано:**
  - **Устранена проблема нерелевантного копирования:** удалены бессмысленные полноразмерные кнопки «Скопировать» с карточек новостей, архитектурных тезисов и теоретических определений, где отсутствуют команды или код.
  - **Интеллектуальная контекстная типизация карточек (`resolveCardData`):**
    1. **Команды терминала (`base` - DevOps):** оформлены в стилизованный консольный блок с заголовком «Команда терминала» и компактной кнопкой «Скопировать команду».
    2. **Код и шпаргалки (`courses` - Web & Backend, `chill` - Интервью):** карточки дополнены понятным теоретическим описанием нормальным читаемым шрифтом (`conceptText`), а реальные примеры кода вынесены в syntax-блок с компактной кнопкой «Скопировать код».
    3. **IT English (`english`):** английская фраза выделена в стилизованный цитатный блок (`phraseCallout`), с переводом и кнопкой «Скопировать фразу» для удобной вставки в рабочий чат или PR.
    4. **Онлайн-инструменты (`tools`):** добавлена кнопка прямого перехода «Открыть сервис» (`handleOpenUrl`) и компактная кнопка «Скопировать ссылку».
    5. **Новости и тренды (`news`):** карточки переведены в формат лаконичного дайджеста с кнопкой «Читать источник» при наличии внешнего ресурса, без лишних кнопок копирования.
    6. **Архитектура (`roadmap`):** паттерны системного дизайна оформлены в виде структурированных инженерных тезисов с маркерами правил.
  - **Кросс-платформенный буфер обмена:** в `App.js` добавлена нативная запись в `navigator.clipboard.writeText` для веб-версии при копировании сниппетов.
  - **Словари i18n:** в `translations.js` добавлены специализированные лейблы кнопок для RU и UZ (`copyCodeBtn`, `copyCommandBtn`, `copyPhraseBtn`, `copyLinkBtn`, `openServiceBtn`, `readSourceBtn`).
- **Архитектурные последствия:** Карточки коллекций стали строго семантическими и контекстно-зависимыми. Устранен визуальный мусор, интерфейс полностью согласован с Neo-Brutalism канонами проекта.

### [2026-09-08 16:08] Удаление устаревшего дубликата dev_learn_mobile и консолидация монорепозитория
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Архитектура / Очистка / Рефакторинг
- **Затронутые модули/файлы:** `/home/alisher/my_projects/dev_learn_mobile` (удалено), `CHANGELOG.md`
- **Что сделано:**
  - Полностью удалена устаревшая папка `/home/alisher/my_projects/dev_learn_mobile` (освобождено ~412 МБ дискового пространства).
  - Устранено избыточное зеркалирование: теперь проект существует строго в едином монорепозитории `/home/alisher/my_projects/dev_learn/` (`api/` + `mobile-app/`).
- **Архитектурные последствия:** Исключена путаница версий между двумя директориями, весь стек платформы (API, Web, Android, iOS) консолидирован в едином корневом проекте `dev_learn`.

### [2026-09-08 16:03] Редизайн экранов уроков под фирменную дизайн-систему Neo-Brutalism
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Рефакторинг / UI/UX / Дизайн-система
- **Затронутые модули/файлы:** `mobile-app/src/screens/LessonScreen.js`, `mobile-app/src/screens/LessonsCatalogScreen.js`, `dev_learn_mobile/` (зеркало)
- **Что сделано:**
  - Полностью переписан UI экранов `LessonScreen` и `LessonsCatalogScreen` для 100% соответствия эталонным стандартам проекта (`CollectionScreen` и `MediaScreen`):
    1. **Шапка экрана (`feedHeader`):** заменена на фирменную нео-брутальную шапку с кнопками `AnimatedIconButton` (`brutalIconButton`, `iconName="arrow-left"`, `iconName="grid"`, `iconSize={20}`, анимация `nudge/bounce`) и бейджем с черной границей `borderWidth: 2.5` и жесткой тенью `2px 2px 0px #000`.
    2. **Фильтры уровней:** внедрены кнопки `AnimatedBrutalButton` с пружинной физикой `hop` (`mediaFilterPill` и `mediaFilterPillActive` с фирменным желтым акцентом `#FDE047`).
    3. **Карточки и контент (`entryCard` / `mediaCard`):** толстые границы `borderWidth: 2.5`, скругление `borderRadius: 16`, глубокие смещенные черные тени `4px 4px 0px #000`, бейджи платформ и уровней `borderWidth: 1.5`.
    4. **Блоки кода и копирование:** код оформлен в моноширинный контейнер `codeSnippetBox` с фирменной желтой кнопкой `AnimatedCopyButton` (`copyBtnBrutal` `#FDE047` с черным текстом и оффсет-тенью).
    5. **Интеграция маскота:** в блок `Dotty Pro-Tip` внедрен живой анимированный векторный талисман `<AnimatedDotty size={42} animated={true} />`.
  - Код проверен бандлером Metro Web и синхронизирован в зеркало `dev_learn_mobile`.
- **Архитектурные последствия:** Устранена визуальная рассинхронизация интерфейса; модуль уроков органично вписался в единый дизайн-код платформы DevLearn.

### [2026-09-08 15:58] Добавление модуля полноформатных интерактивных уроков (React Native & Dart: Junior/Middle/Senior)
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Новая фича / Документация / Обучение
- **Затронутые модули/файлы:** `mobile-app/src/constants/lessonsData.js`, `mobile-app/src/screens/LessonScreen.js`, `mobile-app/src/screens/LessonsCatalogScreen.js`, `mobile-app/src/constants/translations.js`, `mobile-app/App.js`, `dev_learn_mobile/` (зеркало)
- **Что сделано:**
  - Реализован фирменный модуль текстовых интерактивных уроков прямо внутри приложения (без внешних переходов по ссылкам) с градацией по инженерным уровням (Junior / Middle / Senior) и разбором кода:
    1. **База данных уроков (`lessonsData.js`):**
       - **React Native (RN):** от примитивов и StyleSheet до Новой архитектуры (JSI, Fabric, TurboModules), оптимизации `FlatList` (`getItemLayout`, `windowSize`) и профилирования в Hermes.
       - **Dart & Flutter Core:** от строгой статической типизации и Sound Null Safety (`?`, `!`, `late`) до двух очередей Event Loop (Microtasks vs Event Queue), реактивных потоков `Stream` и изолятов (`Isolates` без разделяемой памяти).
       - Для каждого урока добавлены блоки **💡 Dotty Pro-Tip** с каверзными вопросами и разборами с реальных собеседований.
    2. **Экран чтения урока (`LessonScreen.js`):**
       - Интерактивный фильтр переключения уровней (`Все разделы`, `🔰 Junior`, `⚡ Middle`, `🚀 Senior`).
       - Блоки кода с темной темой, моноширинным шрифтом и кнопкой быстрого копирования (`AnimatedCopyButton`).
       - Списки ключевых инженерных правил и архитектурных выводов.
       - Переключение между уроками в нижней панели.
    3. **Каталог уроков (`LessonsCatalogScreen.js`):**
       - Поиск по ключевым словам и темам, бейджи технологий, тайминги чтения.
    4. **Маршрутизация и Web Linking:**
       - Добавлены маршруты `/lessons` и `/lesson/:lessonId`.
       - Динамическое обновление заголовка вкладки браузера (`DevLearn | Урок: REACT-NATIVE`, `DevLearn | Интерактивные Уроки`).
       - На главный экран `HomeScreen` добавлена карточка перехода к урокам.
    5. **Локализация:** полная поддержка RU и UZ языков.
  - Код валидирован через `@babel/parser` и протестирован бандлером Metro/Expo Web (608 модулей собраны без единой ошибки).
  - Изменения синхронизированы в репозиторий `dev_learn_mobile`.
- **Архитектурные последствия:** Платформа DevLearn расширилась от формата кратких шпаргалок до полноценной обучающей базы знаний с глубокими инженерными руководствами и нативным чтением прямо в приложении.

### [2026-09-08 15:35] Переход Backend API на модульную архитектуру (Layered Modular Architecture)
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Архитектура / Рефакторинг / Backend API
- **Затронутые модули/файлы:** `api/server.js`, `api/src/app.js`, `api/src/config/env.js`, `api/src/config/database.js`, `api/src/constants/seedQuestions.js`, `api/src/controllers/cardsController.js`, `api/src/controllers/healthController.js`, `api/src/routes/index.js`, `api/src/routes/cardsRoutes.js`, `api/src/routes/healthRoutes.js`, `api/src/services/cardsService.js`, `api/src/services/sm2Service.js`, `api/src/middlewares/errorHandler.js`, `api/src/middlewares/requestLogger.js`, `api/README.md`
- **Что сделано:**
  - Монолитный файл `api/server.js` (350 строк) рефакторингован в чистую модульную структуру по слоям ответственности:
    1. **Конфигурация (`src/config/`):** `env.js` для безопасной загрузки переменных окружения и абсолютных путей к БД; `database.js` для управления подключением к SQLite через `better-sqlite3`, включения высокопроизводительного режима WAL (`journal_mode = WAL`), миграций и сидинга.
    2. **Константы (`src/constants/`):** `seedQuestions.js` с базой из 41 проверочного вопроса Junior/Middle по 7 IT-направлениям.
    3. **Сервисы (`src/services/`):** `sm2Service.js` с математической реализацией алгоритма интервального повторения SuperMemo SM-2 (`repetitions`, `interval`, `easeFactor`, `nextReviewAt`); `cardsService.js` для бизнес-логики работы с карточками, историей ответов и аналитикой обучения.
    4. **Контроллеры (`src/controllers/`):** `cardsController.js` и `healthController.js` для обработки HTTP-запросов и валидации входных параметров.
    5. **Маршрутизация (`src/routes/`):** `cardsRoutes.js`, `healthRoutes.js` и корневой агрегатор `index.js`.
    6. **Middleware (`src/middlewares/`):** `requestLogger.js` с цветным выводом времени ответа и статус-кодов; `errorHandler.js` со стандартным 4-аргументным обработчиком ошибок Express и 404-хэндлером.
    7. **Точка входа и Express App:** `src/app.js` для инициализации Express-приложения и `server.js` для запуска сервера с поддержкой Graceful Shutdown (`SIGINT`, `SIGTERM`).
  - Добавлены новые полезные эндпоинты: `GET /api/cards/stats` (статистика и точность ответов) и `GET /api/health` (проверка аптайма и подключения к БД).
  - Написана подробная документация архитектуры в `api/README.md`.
  - Все эндпоинты протестированы сквозным HTTP-тестом: статус 200, корректная фильтрация и пересчет интервалов SM-2.
- **Архитектурные последствия:** Кодовая база бэкенда приведена к промышленным стандартам масштабируемости (Clean/Layered Architecture), упростилось добавление авторизации, новых коллекций вопросов и расширение функционала без риска регрессий.

### [2026-09-08 14:10] Переход с ручного состояния экранов (useState) на React Navigation v7 с Web Linking
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Архитектура / Маршрутизация / Web & Mobile
- **Затронутые модули/файлы:** `mobile-app/package.json`, `mobile-app/index.js`, `mobile-app/App.js`, `dev_learn_mobile/` (зеркало)
- **Что сделано:**
  - Устранено непрактичное управление экранами через локальное состояние `useState(currentScreen)`.
  - Внедрен официальный стек маршрутизации `@react-navigation/native` и `@react-navigation/native-stack` v7 с поддержкой глубоких ссылок (Deep Linking) и реальных URL для браузера (Web Linking):
    1. Маршрут Главного экрана: `/` (`Home`).
    2. Маршрут Медиатеки: `/media` (`Media`).
    3. Маршрут Блиц-спринта: `/blitz` (`Blitz`).
    4. Маршрут Коллекций с динамическим параметром: `/collection/:collectionKey` (например, `/collection/courses`, `/collection/base`, `/collection/shortcuts`).
  - Обеспечена полноценная работа кнопок браузера «Назад» и «Вперед» (`history.back()` / `history.forward()`), а также сохранение текущего экрана при перезагрузке страницы по F5.
  - Настроено динамическое обновление заголовка вкладки в браузере (`document.title`) на основе текущего экрана и выбранной коллекции/языка.
  - Все модальные окна (`AddNoteModal`, `SettingsModal`, `AboutModal`), переключатель языка и анимации сохранены в полном объеме.
  - Код валидирован через `@babel/parser` и проверен тестовым бандлингом Expo Web (`npx expo export -p web` — 605 модулей без единой ошибки).
  - Изменения и новые зависимости синхронизированы в зеркало `dev_learn_mobile`.
- **Архитектурные последствия:** Приложение получило полноценную кросс-платформенную маршрутизацию (Web SPA с чистыми URL + нативная навигация для Android/iOS) с сохранением консистентного нео-брутального дизайна без стандартных системных хедеров (`headerShown: false`).

### [2026-09-08 13:16] Настройка конфигурации для автономной сборки Android .APK (EAS Build)
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Конфигурация / Сборка / Мобильное приложение
- **Затронутые модули/файлы:** `package.json`, `mobile-app/package.json`, `mobile-app/app.json`, `mobile-app/eas.json`, `dev_learn_mobile/` (зеркало)
- **Что сделано:**
  - Настроена конфигурация для генерации автономного Android `.apk` файла (не требующего Expo Go):
    1. В [`app.json`](file:///home/alisher/my_projects/dev_learn/mobile-app/app.json) добавлен уникальный идентификатор приложения Android: `"package": "com.alisher.devlearn"`.
    2. Создан файл конфигурации [`eas.json`](file:///home/alisher/my_projects/dev_learn/mobile-app/eas.json) с профилем `preview`, настроенным на сборку именно `.apk` (вместо `.aab` Google Play Bundle).
    3. В корневой и мобильный `package.json` добавлена готовая команда `npm run build:apk`.
  - Изменения синхронизированы в репозиторий `dev_learn_mobile`.
- **Архитектурные последствия:** Появилась возможность сборки физического исполняемого файла `.apk` для прямой установки на любые Android-устройства в один клик.

### [2026-09-08 12:44] Устранение обрезки кнопок фильтров и языков при анимации (overflow fix)
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Багфикс / UI/UX
- **Затронутые модули/файлы:** `mobile-app/src/screens/MediaScreen.js`, `mobile-app/src/components/common/AnimatedBrutalButton.js`, `mobile-app/src/components/common/AnimatedIconButton.js`, `mobile-app/src/components/blitz/AnimatedChoiceButton.js`, `mobile-app/src/components/common/AnimatedCopyButton.js`, `mobile-app/src/components/common/AnimatedSettingsLangBtn.js`, `mobile-app/src/components/common/AnimatedLangToggle.js`, `dev_learn_mobile/` (зеркало)
- **Что сделано:**
  - Устранено срезание верхушек кнопок фильтров и языков во время анимации подпрыгивания (`hop`) и покачивания (`wiggle`):
    1. **Вертикальный запас контейнера:** в `mediaFilterContainer` и новом `mediaLangFilterContainer` вертикальный отступ увеличен с `2px` до `8px` (`paddingTop: 8`, `paddingBottom: 8`, `paddingHorizontal: 4`), предоставив элементам свободное пространство внутри скролл-вьюпорта.
    2. **Отключение обрезки div:** для `ScrollView` и родительских контейнеров задано `style={{ overflow: 'visible' }}`.
    3. **Калибровка амплитуды анимаций:** в `AnimatedBrutalButton`, `AnimatedIconButton`, `AnimatedChoiceButton`, `AnimatedCopyButton`, `AnimatedSettingsLangBtn` и `AnimatedLangToggle` амплитуда подпрыгивания скорректирована с экстремальных `-7/-8px` до комфортных `-3.5px`, сохраняя тактильный пружинный отклик (`tension: 190, friction: 3.8`) без выхода элементов за пределы своего контейнера.
  - Изменения валидированы парсером Babel и синхронизированы в репозиторий `dev_learn_mobile`.
- **Архитектурные последствия:** Анимации элементов скроллируемых списков и рядов кнопок воспроизводятся чисто без визуальных артефактов и обрезки на Web, iOS и Android.

### [2026-09-08 12:38] Исправление закрывающего тега в AnimatedLangToggle.js
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Багфикс / Сборка
- **Затронутые модули/файлы:** `mobile-app/src/components/common/AnimatedLangToggle.js`, `dev_learn_mobile/src/components/common/AnimatedLangToggle.js` (зеркало)
- **Что сделано:**
  - Исправлена ошибка парсинга JSX `Expected corresponding JSX closing tag for <Animated.View>`: заменён закрывающий тег `</View>` на `</Animated.View>`.
  - Все ключевые компоненты приложения (14 файлов) валидированы через `@babel/parser` на отсутствие JSX-ошибок.
- **Архитектурные последствия:** Устранена ошибка бандлинга Web-клиента Expo/Metro.

### [2026-09-08 12:35] Интерактивные анимации кнопок («подпрыгивание» и «дёргание») в стиле Neo-Brutalism
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Новая фича / UI/UX Анимации
- **Затронутые модули/файлы:** `mobile-app/src/components/common/AnimatedBrutalButton.js`, `mobile-app/src/components/common/AnimatedIconButton.js`, `mobile-app/src/components/common/AnimatedCopyButton.js`, `mobile-app/src/components/common/AnimatedSettingsLangBtn.js`, `mobile-app/src/components/common/AnimatedLangToggle.js`, `mobile-app/src/components/blitz/AnimatedChoiceButton.js`, `mobile-app/src/components/home/AnimatedFolderCard.js`, `mobile-app/src/screens/MediaScreen.js`, `mobile-app/src/screens/BlitzScreen.js`, `mobile-app/src/screens/CollectionScreen.js`, `mobile-app/src/modals/AddNoteModal.js`, `mobile-app/src/modals/SettingsModal.js`, `mobile-app/src/modals/AboutModal.js`, `dev_learn_mobile/` (зеркало)
- **Что сделано:**
  - Создан универсальный анимированный компонент [`AnimatedBrutalButton`](file:///home/alisher/my_projects/dev_learn/mobile-app/src/components/common/AnimatedBrutalButton.js) с аппаратным ускорением (`useNativeDriver: true`) и физикой Neo-Brutalism:
    1. **«Подпрыгивает» (`hop`)**: при клике кнопка делает резкий прыжок вверх (`translateY: -7px`, `scale: 1.04`) и пружинит обратно на своё место.
    2. **«Дёргается» (`wiggle` / `twitch`)**: при нажатии кнопка задорно покачивается и вибрирует влево-вправо (`-4deg` .. `+4deg`) с лёгким подскоком.
    3. **«Необруталистский отскок» (`bounce`)**: продавливание в тень (`pressX: 2`, `pressY: 2`) и мощный пружинный прыжок вверх при отпускании.
  - Обновлён [`AnimatedIconButton`](file:///home/alisher/my_projects/dev_learn/mobile-app/src/components/common/AnimatedIconButton.js): добавлены типы `'hop'`, `'wiggle'`, `'twitch'` и плавный поворот `'spin'` (на 360° для настроек и сброса).
  - Обновлён [`AnimatedChoiceButton`](file:///home/alisher/my_projects/dev_learn/mobile-app/src/components/blitz/AnimatedChoiceButton.js): при ответе в Блиц-Спринте кнопка «Ложь» или «Правда» целиком подпрыгивает и наклоняется.
  - Обновлён [`AnimatedCopyButton`](file:///home/alisher/my_projects/dev_learn/mobile-app/src/components/common/AnimatedCopyButton.js): при копировании кода кнопка подпрыгивает и вздрагивает.
  - Обновлены кнопки переключения языков [`AnimatedSettingsLangBtn`](file:///home/alisher/my_projects/dev_learn/mobile-app/src/components/common/AnimatedSettingsLangBtn.js) и [`AnimatedLangToggle`](file:///home/alisher/my_projects/dev_learn/mobile-app/src/components/common/AnimatedLangToggle.js): добавлен подпрыгивающий отклик.
  - Обновлена карточка папок [`AnimatedFolderCard`](file:///home/alisher/my_projects/dev_learn/mobile-app/src/components/home/AnimatedFolderCard.js): пружинный отскок при отпускании.
  - Оснащены интерактивной анимацией все кнопки экранов и модальных окон: «Смотреть / Открыть», плашки фильтров медиа, «Следующий вопрос», «Добавить заметку в базу», «Закрыть», «Вернуться в меню», плашки выбора категорий.
- **Архитектурные последствия:** Приложение получило сочный, тактильный отклик интерфейса (micro-interactions) на Web, iOS и Android без потерь производительности благодаря использованию нативного драйвера.

### [2026-09-08 12:25] Багфикс UI плашек фильтров экрана «Медиа & Видео»
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Багфикс / UI/UX
- **Затронутые модули/файлы:** `mobile-app/src/screens/MediaScreen.js`, `dev_learn_mobile/src/screens/MediaScreen.js` (зеркало)
- **Что сделано:**
  - Исправлен дефект отображения кнопок фильтров (`Все`, `Курсы`, `Видео`, `Подкасты`, `Дайджесты` и фильтры языков):
    1. Кнопкам `mediaFilterPill` и `mediaLangFilterPill` добавлено `flexDirection: 'row'`, `alignItems: 'center'` и `justifyContent: 'center'`. Ранее из-за дефолтного `column` в React Native векторная иконка и текст рендерились вертикально в столбик, раздувая высоту и схлопывая плашки.
    2. Убран фиксированный ограничитель высоты (`height: 38` и `height: 34`), который обрезал верхние и нижние границы плашек (из-за чего отображались только боковые скобки `[ ]`). Контейнерам заданы гибкие отступы `marginBottom: 10/12` и внутренний паддинг `paddingVertical: 2`.
    3. Исправлена контрастность иконок: активная иконка установлена в контрастный черный цвет `#000000` (вместо белого `#FFFFFF` на голубом фоне `#38BDF8`), гармонично сочетаясь с текстом и Neo-Brutalist обводкой.
  - Изменения проверены и синхронизированы с репозиторием `dev_learn_mobile`.
- **Архитектурные последствия:** Восстановлена целостность верстки фильтрации медиаресурсов на Web и мобильных платформах в соответствии с дизайн-системой Neo-Brutalism.

### [2026-09-08 12:20] Унификация дизайн-системы иконок по всему проекту (Feather Vector Icons)
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Рефакторинг UI/UX / Дизайн-система
- **Затронутые модули/файлы:** `mobile-app/src/constants/collectionsData.js`, `mobile-app/src/constants/translations.js`, `mobile-app/src/constants/mediaResources.js`, `mobile-app/src/constants/blitzQuestions.js`, `mobile-app/src/constants/mascotTips.js`, `mobile-app/App.js`, `mobile-app/src/screens/MediaScreen.js`, `mobile-app/src/screens/BlitzScreen.js`, `mobile-app/src/screens/HomeScreen.js`, `mobile-app/src/modals/SettingsModal.js`, `mobile-app/src/modals/AddNoteModal.js`, `mobile-app/src/modals/AboutModal.js`, `mobile-app/src/components/common/AnimatedSettingsLangBtn.js`, `mobile-app/src/components/common/LangSwitchToast.js`, `dev_learn_mobile/` (зеркало)
- **Что сделано:**
  - Проведена полная стандартизация иконографики: ликвидирован визуальный разнобой между нативными цветными системными эмодзи (пингвины, телевизоры, огоньки, флаги стран, галочки) и векторными иконками.
  - Утвержден единый стиль иконок для всего приложения — **Feather Vector Icons** (минималистичный 2px stroke, круглые окончания, монохромный вектор):
    1. **Главный экран и карточки:** убраны эмодзи из поля `extra` карточек (`🐧 Серверы`, `⚡ Стек`, `🎯 Вопросы` и т.д. заменены на аккуратный локализованный текст `Linux & Серверы`, `Frontend & Бэкенд`, `Интервью & Задачи`, `Серия xN`), сохранены чистые Feather иконки в квадратных блоках.
    2. **Экран «Медиа & Видео»:** табы категорий переведены на строгие векторные иконки Feather (`grid`, `book-open`, `video`, `mic`, `rss`) вместо разномастных эмодзи; языковые фильтры переведены на Feather `globe` и лаконичные бейджи `UZ`, `RU`, `EN`.
    3. **Блиц-Спринт:** в плашке серии комбо эмодзи огонька заменен на векторную иконку `zap`; в баннере ответа добавлены векторные индикаторы `check-circle` и `alert-circle`. Из базы вопросов удалены текстовые символы `✅` и `❌`.
    4. **Модальное окно добавления заметки:** плашки выбора категорий теперь рендерят оригинальные векторные иконки Feather каждой коллекции (`terminal`, `code`, `award`, `layers`, `globe`, `tool`, `radio`).
    5. **Настройки и тоасты:** флаги и текстовые галочки заменены на компактные Neo-Brutalism бейджи кодов языков (`RU`/`UZ`) и векторные галочки Feather `check` и `check-circle`.
  - Все изменения протестированы и синхронизированы в репозиторий `dev_learn_mobile`.
- **Архитектурные последствия:** Достигнута визуальная целостность и единая дизайн-система во всех экранах и компонентах без кроссплатформенных артефактов рендеринга разномастных системных эмодзи.

---

### [2026-09-08 12:10] Семантическая реорганизация структуры папок и коллекций знаний
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Архитектура / Рефакторинг UX/UI
- **Затронутые модули/файлы:** `mobile-app/src/constants/collectionsData.js`, `mobile-app/App.js`, `mobile-app/src/components/home/AnimatedFolderCard.js`, `mobile-app/src/constants/translations.js`, `mobile-app/src/screens/CollectionScreen.js`, `mobile-app/src/modals/AddNoteModal.js`, `dev_learn_mobile/` (синхронизация зеркала)
- **Что сделано:**
  - Устранена путаница и дублирование в папках главного экрана: ранее категории «Без категории» (где лежали Linux и Docker), «Видео & Курсы», «Обучение & Стек», «Интервью & Задачи» и «Блиц-Лента» пересекались по смыслу и сбивали с толку.
  - Разделены все 9 блоков по четким семантическим ролям и дополнены емкими подзаголовками стека и технологий:
    1. `feed`: **Блиц-Спринт** (`zap`, бейдж `SPRINT`, сабтайтл *«Скоростные тесты»*) — быстрый интерактивный тренажер тестов.
    2. `media`: **Медиа & Видео** (`play-circle`, бейдж `MEDIA`, сабтайтл *«YouTube & Открытые курсы»*) — открытые лекции, курсы и подкасты.
    3. `base`: **DevOps & Серверы** (`terminal`, бейдж `DEVOPS`, сабтайтл *«Linux, Docker, Nginx, Git»*) — серверный стек и bash команды (ранее размытая папка «Без категории»).
    4. `courses`: **Web & Backend Стек** (`code`, бейдж `CORE`, сабтайтл *«React, TS, Node, NestJS»*) — рантайм, хуки, типизация и паттерны веб-разработки.
    5. `chill`: **Разбор Интервью** (`award`, бейдж `INTERVIEW`, сабтайтл *«Closure, EventLoop, SQL»*) — каверзные вопросы и подводные камни с собеседований.
    6. `roadmap`: **Архитектура & БД** (`layers`, бейдж `DESIGN`, сабтайтл *«System Design, SQL, Redis»*) — паттерны проектирования, шардирование и базы данных.
    7. `english`: **IT Английский** (`globe`, бейдж `ENGLISH`, сабтайтл *«Daily, Code Review, Slack»*) — профессиональный лексикон для дейли и PR.
    8. `tools`: **Тулзы & Утилиты** (`tool`, бейдж `TOOLS`, сабтайтл *«Regex, Bundle, Crontab»*) — полезные внешние сервисы, калькуляторы и чеклисты.
    9. `news`: **Новости & Тренды** (`radio`, бейдж `TRENDS`, сабтайтл *«React 19, AI, Highload»*) — релизы, технологии и AI-тренды.
  - В карточке `AnimatedFolderCard.js` реализован вывод подзаголовка (`item.subtitle`) с указанием конкретных технологий в Neo-Brutalist стиле.
  - В шапке `CollectionScreen.js` добавлен рендеринг подзаголовка папки перед описанием.
  - В модальном окне `AddNoteModal.js` селектор категорий теперь отображает наглядные теги с эмодзи (`extra` / `extra_uz`) вместо обрезанных названий.
  - В глобальном поиске главного экрана (`App.js`) поиск теперь работает не только по заголовку и метаданным, но и по подзаголовкам стека (`card.subtitle`), позволяя находить папки по ключевым словам (например, «docker», «react», «sql»).
  - Удалена неиспользуемая пустая папка `src/components/navigation`.
  - Все изменения синхронизированы в репозиторий `dev_learn_mobile`.
- **Архитектурные последствия:** Достигнута 100% смысловая чистота классификатора знаний. Устранены пересекающиеся названия, улучшен поиск и UX категоризации заметок без нарушения обратной совместимости ключей хранилища.

---

### [2026-09-08 11:55] Багфикс кнопки «Инфо» на экране «Видео & Курсы» (Web + Mobile)
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Багфикс / Новая фича
- **Затронутые модули/файлы:** `mobile-app/src/screens/MediaScreen.js`
- **Что сделано:**
  - Исправлен сбой/некликабельность кнопки информации в верхнем правом углу экрана «Видео & Курсы»: ранее она вызывала `Alert.alert()`, который не работает в браузере под `react-native-web`.
  - Реализовано стильное Neo-Brutalist диалоговое окно (`infoModal`), которое открывается как на Web, так и на iOS/Android при нажатии на кнопку инфо.
  - Окно содержит подробное описание преимуществ раздела: 100% бесплатный доступ к открытым материалам (YouTube, Stepik, Habr), отсутствие расхода памяти устройства, поддержка 3 языков (UZ, RU, EN) и кнопку подтверждения «Всё понятно ⚡» / «Tushunarli ⚡».
- **Архитектурные последствия:** Ликвидирован платформозависимый вызов нативного Alert в пользу универсального кроссплатформенного UI-компонента на базе `<Modal>`.

---

### [2026-09-08 11:45] Адаптивный респонсив-дизайн для Web и ликвидация мокапа телефона
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Архитектура / Рефакторинг UI
- **Затронутые модули/файлы:** `mobile-app/App.js`, `mobile-app/src/screens/HomeScreen.js`, `mobile-app/src/screens/CollectionScreen.js`, `mobile-app/src/screens/BlitzScreen.js`, `mobile-app/src/screens/MediaScreen.js`, `mobile-app/src/components/home/AnimatedFolderCard.js`, `mobile-app/src/modals/SettingsModal.js`, `mobile-app/src/modals/AboutModal.js`, `mobile-app/src/modals/AddNoteModal.js`
- **Что сделано:**
  - Полностью убран бутафорский мокап iPhone (`phoneFrame` с жесткими `390x844px`, скруглениями 44px, рамкой и фейковым статус-баром `9:41`).
  - Внедрен полноценный адаптивный контейнер `appContainer` с `maxWidth: 1160px`, центрированием на мониторах ПК и чистым фоном `#F8F9FA`.
  - Сетка карточек на главном экране переведена на динамический расчет колонок через `useWindowDimensions`: 4 карточки в ряд на десктопе (>= 1024px), 3 карточки на планшетах (>= 720px) и 2 карточки на смартфонах.
  - Ограничена ширина контента и добавлено центрирование для экранов `CollectionScreen` (860px), `BlitzScreen` (720px) и `MediaScreen` (960px).
  - Модальные окна (`SettingsModal`, `AboutModal`, `AddNoteModal`) адаптированы: на десктопе отображаются как стильные центрированные Neo-Brutalist диалоговые окна с тенями вместо мобильных нижних шторок (bottom sheets).
- **Архитектурные последствия:** Приложение теперь выглядит как полноценный десктопный веб-сервис на больших экранах, сохраняя 100% нативный мобильный вид на смартфонах.

---

### [2026-09-08 11:28] Введение строгого регламента версионирования для AI и разработчика
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Архитектура / Конфигурация процессов
- **Затронутые файлы:** `CHANGELOG.md`, `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `.cursorrules`
- **Что сделано:** 
  - Создан единый журнал проекта `CHANGELOG.md`.
  - Внедрены конфигурационные правила для всех LLM/AI ассистентов (Gemini, Claude, GPT), обязывающие логировать абсолютно каждое изменение в коде и архитектуре перед ответом пользователю.
- **Архитектурные последствия:** Ни одно изменение в кодовой базе больше не теряется; контекст проекта прозрачен для любого подключенного AI.

---

### [2026-09-08 11:17] Удаление устаревшего `client/` (Frontend) и переход на 2-компонентную архитектуру
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Архитектура / Очистка
- **Затронутые файлы:** `client/` (удален), `package.json`, `README.md`
- **Что сделано:**
  - Полностью удалена папка `client/` (React 19 + Vite), так как она сильно отстала от мобильного приложения по функционалу и дизайну.
  - Обновлен корневой `package.json`: убраны неактуальные команды `dev:client` и `build:client`, добавлена команда `dev:web` (`npm run web --prefix mobile-app`).
  - Веб-версия теперь собирается напрямую из `mobile-app` через `react-native-web`.
- **Архитектурные последствия:** Ликвидировано дублирование кода. Экосистема сведена к двум компонентам: `api/` (бэкенд) + `mobile-app/` (кроссплатформенный клиент: Android + iOS + Web).

---

### [2026-09-08 10:59] Создание генеральной технической документации
- **Автор:** AI (Gemini / Antigravity)
- **Тип:** Документация
- **Затронутые файлы:** `README.md`
- **Что сделано:**
  - Написано подробное техническое описание проекта: стек, библиотеки, ASCII-дерево файлов, алгоритм интервальных повторений (SuperMemo SM-2), правила дизайн-системы Neo-Brutalism и маскота Dotty.
- **Архитектурные последствия:** Зафиксированы стандарты и структура модулей.

---

### [2026-09-08 10:48] Диагностика и устранение сбоя песочницы Electron на Ubuntu 24.04 (`SIGTRAP` Signal 5)
- **Автор:** AI (Gemini / Antigravity) & Разработчик (alisher)
- **Тип:** Багфикс / Окружение
- **Затронутые файлы:** `~/.zshrc`, `/etc/sysctl.d/20-apparmor-donotrestrict.conf`
- **Что сделано:**
  - Локализована причина падения `React Native DevTools` (AppArmor в Ubuntu 24.04 блокирует `CLONE_NEWUSER`).
  - Применено решение через `kernel.apparmor_restrict_unprivileged_userns = 0` и переменную `ELECTRON_DISABLE_SANDBOX=1`.
- **Архитектурные последствия:** Обеспечена стабильная работа инструментов отладки Expo/React Native в среде Linux Ubuntu 24.04.

---

### [Начальное состояние] Создание монорепозитория DevLearn
- **Автор:** Разработчик (alisher)
- **Тип:** Архитектура
- **Затронутые файлы:** `api/`, `mobile-app/`, `package.json`
- **Что сделано:**
  - Разработан REST API на Express 5 + SQLite (`better-sqlite3`) с SRS-интервалами.
  - Разработано мобильное приложение на Expo 57 / React Native 0.86 с дизайном Neo-Brutalism, экранами `HomeScreen`, `CollectionScreen`, `BlitzScreen`, `MediaScreen`, модальными окнами и маскотом Dotty.
